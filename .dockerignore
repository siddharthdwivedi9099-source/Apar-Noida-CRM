# Dependencies and build output are reinstalled/rebuilt inside the image.
**/node_modules
**/dist
**/coverage

# VCS and CI
.git
.gitignore
.github

# Secrets and local env (never bake into images). The example stays for reference.
.env
.env.*
!.env.example

# Editor / OS noise
.DS_Store
*.log
.vscode
.idea
