# CRM Configuration Roadmap

A **safe, additive** plan to take the CRM from "configurable core + automations" to **fully configurable**, without rewriting the app or hard-coding workflows. Read alongside [configuration-engine-audit.md](./configuration-engine-audit.md).

---

> **Delivered since this roadmap was written:**
> - *Configuration-versioning phase* — the cross-cutting backbone: versioning/publishing/rollback, JSON import/export with dependency validation, a pure validation/safety engine, and a typed field-attributes contract.
> - *Apply-to-live phase* — publishing a version can now be **applied onto live config tables** (upsert-only, single transaction, validation-gated, with a pre-apply backup + audit), so configuration actually drives the app. **Still open here:** form-layout application and guarded orphan removal.
> - *Configuration-definitions phase* — a generic `configuration_definitions` registry now makes module metadata, objects, page layouts, business-process flows, approval matrices, notification rules, and dashboard compositions versioned, validated, published, and applied as governed configuration. Runtime rendering/execution remains intentionally layered on top in later phases.
> - *Core CRM metadata phase* — the requested CRM modules, core objects, relationships, flags, baseline views/forms, and standard picklists are seeded as configuration metadata rather than hard-coded UI/business logic.
> - *Persona access metadata phase* — 32 CRM personas, access policies, field/record/action permission metadata, and role-based page-layout definitions are seeded through the configuration engine. Enforcement of the richer record/field rules remains a later runtime phase.
> - *Business process flows phase* — all 6 CRM BPFs (Lead, Opportunity, Campaign, Partner, Support, Customer Success) are configured as governed `business_process_flow` definitions: every stage, order, required field, allowed/blocked transition, SLA/aging threshold, and notification/task/approval/AI-trigger reference is data, not hard-coded. The BPF contract gained entry/terminal flags and the validator enforces single-entry, terminal-no-outgoing, and reference integrity.
> - *BPF runtime + stage UI phase* — a pure transition validator (`validateBpfTransition`) + SLA/aging compute power a runtime service (`/bpf/:object/:recordId/state|history|transition`) over additive `bpf_record_state`/`bpf_stage_history` tables: stage transitions are enforced against the configured BPF (allow-list/blocked/default-deny, required-fields-by-stage, **closed-stage locking**, **backward-movement reason**, **manager override**), every change is recorded to history + audit. A reusable `StageProgress` component (stage bar, validation messages, aging/SLA indicator, history, override) is wired into the Lead and Opportunity detail pages. **Per-stage notification/approval dispatch and the remaining object detail pages are the next slice.**
> - *Lead config foundation phase (PROMPT 05.1)* — the full lead field catalog (designation, industry, region, segment, product interest, sub-source, UTM + first/latest-touch attribution, consent, fit/engagement/intent/AI scores, grade, qualification + disqualification status, SLA/assignment/follow-up dates) is configured on the lead `object` definition with searchable/filterable/reportable/AI-usable/sensitive/masking attributes, plus six new governed picklists (capture-source, sub-source, grade, qualification-status, disqualification-reason, consent-status). Configuration-only; scoring/assignment/dedup/conversion/UI are sub-phases 05.2–05.7.
> - *Lead scoring + MQL phase (PROMPT 05.2)* — `scoring_model` and `mql_rule` governed definition types + a pure evaluator (`evaluateLeadScore` → per-dimension scores, weighted final score, grade, and a matched/unmatched breakdown; `evaluateMql` → MQL decision with reasons). A default lead scoring model (fit/engagement/intent/AI) and MQL rule (threshold + required fields + consent criterion) are seeded as configuration. Rules/weights/grades/threshold are all data, not hard-coded. Runtime compute/history API folds into the lead runtime/UI slice.
> - *Lead assignment + SLA phase (PROMPT 05.3)* — `assignment_rule` and `sla_policy` governed definition types + pure resolvers (`resolveAssignmentRule` picks the first priority/condition-matching rule with its strategy + pool/target, else fallback; `computeSlaStatus` computes due time + ok/warning/breached/met per SLA target). Seeded a default lead assignment rule set (partner/segment/region/round-robin + fallback queue) and SLA policy (first-response 4h, follow-up 24h, manager escalation). Round-robin/load-balanced pool *selection* and breach dispatch are runtime concerns left to the lead runtime/worker slice.
> - *Lead runtime guidance phase (PROMPT 05.4)* — `GET /leads/:leadId/runtime` now composes active `scoring_model`, `mql_rule`, `assignment_rule`, and `sla_policy` definitions for a live lead and returns read-only scoring, MQL, assignment, and SLA guidance. The Lead detail page displays that guidance. It does **not** mutate ownership, persist score history, or dispatch SLA escalations yet; those remain worker/runtime-history slices.
> - *Lead conversion phase (PROMPT 05.5)* — `POST /leads/:leadId/convert` now performs the first conversion slice: it checks lead qualification readiness, runs duplicate detection for accounts and contacts, auto-links strong unambiguous matches or creates missing account/contact records, creates an opportunity plus a handoff task, updates the lead status to `converted`, and stores a conversion summary back on the lead. This is a deterministic operational flow, not a full merge console: fuzzy duplicate resolution and richer merge governance remain a later data-quality slice.
> - *Lead + account + opportunity custom-field values phase (Phase A vertical slice)* — `leads.custom_fields`, `accounts.custom_fields`, and `opportunities.custom_fields` are now persisted end-to-end. `POST/PATCH /leads`, `POST/PATCH /accounts`, and `POST/PATCH /opportunities` accept `customFields`, the corresponding detail endpoints return persisted custom-field values, the options endpoints return effective `fieldDefinitions` plus option catalogs for custom select/multiselect fields, the Lead, Account, and Opportunity form/detail UI now render and save active tenant-defined fields, and the lead runtime evaluator sees persisted lead custom fields without letting them override core system fields.
> - *Contact custom-field values phase (Phase A continuation)* — `contacts.custom_fields` is now persisted end-to-end on the same additive JSONB pattern. `POST/PATCH /contacts` accept `customFields`, the contact detail endpoint returns persisted values, `GET /contacts/options` returns effective `fieldDefinitions` plus option catalogs for custom select/multiselect fields, and the Contact form/detail UI render and save active tenant-defined fields. Reuses the shared `fieldDefinitions` + custom-field-options contract rather than a new payload shape, proving it as a platform pattern across four CRM entities.
> - *Support ticket custom-field values phase (Phase A — first non-CRM-service entity)* — `support_tickets.custom_fields` is persisted end-to-end (`entity_key = "support_ticket"`). The custom-field engine was extracted into a shared, entity-agnostic module (`apps/api/src/common/custom-fields.ts` — load definitions, build option catalogs, validate/merge values) so modules outside `CrmService` can adopt it without duplicating validation. `POST/PATCH /support/tickets` accept `customFields`, the ticket detail returns persisted values, `GET /support/options` returns effective `fieldDefinitions` plus option catalogs, and the support workspace renders/saves active tenant-defined fields via a reusable `CustomFieldInput` component. The shared engine has unit tests; the support exhaustive suite proves a ticket custom field round-trips through create + update.
> - *Partner custom-field values phase (Phase A continuation)* — `partners.custom_fields` is persisted end-to-end (`entity_key = "partner"`) on the shared `apps/api/src/common/custom-fields.ts` engine. `POST/PATCH /partners` accept `customFields`, the partner detail returns persisted values, `GET /partners/options` returns effective `fieldDefinitions` plus option catalogs, and the partner workspace renders/saves active tenant-defined fields and displays them on the detail card via the reusable `CustomFieldInput` component. The partner exhaustive suite proves a partner custom field round-trips through create + update. Custom fields now span six entities (lead, account, contact, opportunity, support ticket, partner).
> - *Inside Sales Representative phase (Persona 6 — ISR-001/002/004/006)* — the existing inside-sales workspace was extended on the configuration engine rather than hard-coded. ISR-001: the lead queue now surfaces configurable priority + hot-lead highlighting (derived from the lead scoring grade + the configured lead `sla_policy`), per-lead first-response SLA due time, product/solution context, and lead score/grade, and is sorted hottest/most-urgent first; `best_contact_recommendation` and `lead_summary` join the governed AI placeholders. ISR-002: a new tenant-configurable `lead-contact-script` option set drives guided first-contact scripts resolved per lead by product/source/persona (`resolveContactScript`), the `lead-call-disposition` set gained the `wrong_number`/`interested`/`callback_requested` outcomes, and logging a disposition auto-creates the configured next-step task from the disposition's `metadata.nextStep`. ISR-004: a new configurable `lead-qualification-checklist` option set (with `metadata.required`) replaces a hard-coded list — required items must be complete to mark a lead qualified unless an override reason is supplied, plus a qualification outcome and `qualification_outcome_suggestion` AI placeholder. ISR-006: disqualification now requires a reason from the configurable `disqualification-reason` set, routes `future_need` leads to nurture, and excludes disqualified leads from active SLA. All script/checklist/disposition logic is data (option-set values + metadata), seeded for new tenants and backfilled idempotently for existing tenants; pure resolvers live in `packages/types/src/lead-inside-sales.ts` with unit tests.
> - *Inside Sales Representative phase — completion (ISR-002 campaign + ISR-003 + ISR-005)* — closes the remaining Persona 6 gaps on the same engine. ISR-002: first-contact scripts gained a **campaign** targeting dimension (`metadata.campaignKey`, resolved from lead `campaignType`/`campaignKey` metadata) so scripts vary by product/campaign/source/persona. ISR-003: a new tenant-configurable `lead-cadence-step` option set (each value carries `metadata.channel` + `metadata.offsetHours`) drives a multi-channel contact cadence (call/email/sms/whatsapp/linkedin/follow-up) computed per lead by the pure `evaluateLeadCadence`; reps advance steps, pause with a mandatory reason, and log failed attempts — crossing the failed-attempt threshold auto-routes the lead to nurture; SLA-breach is surfaced as a per-lead `slaBreachAlert`. ISR-005: a new `lead-meeting-type` option set + `POST /sales-workspaces/leads/:leadId/meetings` books a meeting — it writes a `meeting` activity carrying agenda/participants/CRM-record link, creates a meeting task plus a reminder task, and moves the lead into a new configurable `meeting_scheduled` lead status; calendar-invite/email delivery remains a governed placeholder until the scheduling/outbound runtime lands. New option sets are seeded for new tenants and backfilled idempotently for existing ones; cadence + campaign resolvers are unit tested. Persona 6 (ISR-001–006) is now complete.
> - *Sales Development Representative phase (Persona 7 — SDR-001…006)* — extends the shared sales workspace + lead-runtime engine. SDR-001: account research (company profile, leadership, signals, talking points) is captured on the lead with logged sources + a visible confidence; AI generation is a governed placeholder. SDR-002: ICP fit is computed by the pure `evaluateIcpFit` over captured attributes (industry/segment/size/geography/use-case/budget/strategic-value) weighted by a configurable `lead-icp-criterion` set → high/medium/low band + an explanation breakdown, with an ICP fit distribution surfaced on the SDR dashboard. SDR-003: a configurable `lead-discovery-field` set drives a structured discovery form whose required fields are enforced before conversion (`evaluateDiscovery`); AI summary/follow-up email are placeholders. SDR-004: the existing `POST /leads/:leadId/convert` flow (account/contact create-or-link, duplicate detection, opportunity + handover task, status→converted) is surfaced as a workspace convert action. SDR-005: `POST /sales-workspaces/leads/:leadId/no-show` tracks a no-show count, creates a reschedule task, and routes the lead to nurture after the configured threshold. SDR-006: a configurable `lead-objection-type` set captures objections on the lead, with objection trends aggregated on the SDR dashboard; AI objection-handling content is a placeholder. New option sets seed for new tenants + backfill idempotently for existing ones; ICP + discovery resolvers (`packages/types/src/lead-sdr.ts`) are unit tested. Persona 7 (SDR-001–006) is now complete.
> - *Business Development Representative phase (Persona 8 — BDR-001…005)* — extends the existing business-development (target account) module. BDR-001: target accounts gained configurable **priority** (`bd-account-priority`) + **technology** (`bd-technology`) segmentation plus a **bulk-import** endpoint with duplicate-name detection and an AI high-potential placeholder. BDR-002: stakeholders gained a configurable **buyer-role** taxonomy (`bd-buyer-role`: decision-maker/influencer/…/executive) and a pure **buying-committee completeness** score (`evaluateBuyingCommitteeCompleteness`) with missing-role surfacing + AI gap placeholder. BDR-003: a configurable `bd-sequence-step` set drives a multi-channel **outbound sequence** (email/call/LinkedIn/WhatsApp/SMS/task) resolved per account by persona/product/region (`evaluateBdSequence`); steps advance, a logged **reply pauses** the sequence, and AI message drafting is a placeholder. BDR-004: a pure **account engagement score** (`evaluateBdEngagement`) aggregates opens/clicks/visits/event/replies/meetings/stakeholder signals into a cold/warming/hot band + buying-signal flag, an engagement distribution-ready view, and a **convert engaged account → opportunity** action; AI buying-signal detection is a placeholder. BDR-005: a **strategic handoff** action notifies the receiving sales owner (notifications service) and, when requested, opens a **manager approval** (new `strategic_handoff_approval` type on the approvals service), bundling research/stakeholder-map/pain/engagement/approach. New BDR state is stored in the existing `bd_target_accounts`/`bd_account_stakeholders` `metadata` JSONB (no new tables); option sets seed for new tenants + backfill idempotently; sequence/engagement/committee resolvers (`packages/types/src/bd-resolvers.ts`) are unit tested. AI generation and email/WhatsApp delivery remain governed placeholders. Persona 8 (BDR-001–005) is now complete.
> - *Account Executive phase (Persona 9 — AE-001…010)* — extends the opportunities module; all AE state is stored in the opportunity `metadata.salesExec` (and stakeholder profiles keyed by contact id), reusing the SDR discovery resolver (`evaluateDiscovery`) + BD committee resolver (`evaluateBuyingCommitteeCompleteness`), the approvals service (proposal/discount + new `opportunity_reactivation_approval`), notifications, and CRM tasks. AE-001 accept/reject endpoints (reject reason → owner reassignment/queue notification, accept starts the sales SLA, both audit-logged). AE-002 the detail surfaces a full `execWorkspace` and configurable stage gating (`opportunity-pipeline` stage `metadata.requiredFields`). AE-003 stakeholder mapping (role/influence/sentiment/relationship) with buying-committee completeness + missing-role alerts. AE-004 configurable `opportunity-discovery-field` discovery with completeness + critical-field tokens for forward-movement gating. AE-005 presales demo request (task + notification to the presales owner) with status/feedback. AE-006 proposal generation (configurable `opportunity-proposal-template`) with optional `proposal_approval`. AE-007 discount request routed through `discount_approval`; close-won is blocked while a discount approval is pending. AE-008 negotiation tracking. AE-009 close-won (required fields, won outcome, onboarding task + CSM notification, forecast via won amount). AE-010 close-lost (mandatory `loss-reason`, competitor, revisit date) with approval-gated reactivation. Three new option sets seed for new tenants + backfill idempotently; AE write-actions UI is isolated in `opportunity-exec-actions.tsx`. AI drafting/risk/lessons remain governed placeholders. Persona 9 (AE-001–010) is now complete.
> - *Enterprise Sales phase (Persona 10 — ES-001 to ES-005)* — spans accounts + opportunities, all state in existing `metadata` JSONB; reuses approvals/notifications/tasks. ES-001 strategic account plan (overview/BUs/stakeholders/systems/pain/opps/competitors/revenue/risks/action) in account metadata with a manager-review notification + AI whitespace placeholder. ES-002 multi-opportunity: `parentOpportunityId` link + child list + roll-up (total + weighted + by-stage, phased by close date) on the opportunity detail. ES-003 RFP/tender: tender fields + a configurable `opportunity-tender-checklist` (missing-document flags) + function task generation (sales/bid/presales/legal/finance/leadership) + AI summary placeholder. ES-004 executive engagement: CXO meeting notes/commitments/follow-ups + an engagement score on the account. ES-005 strategic deal governance: a review scorecard (solution-fit/pricing/legal/risk/delivery/leadership) required above a value threshold, a new `deal_review_approval` type, and a hard gate blocking stage to negotiation until the review is complete. One new option set seeds for new tenants + backfills idempotently; enterprise UI is isolated in `opportunity-enterprise-actions.tsx` and `account-enterprise-panel.tsx`. AI items remain governed placeholders. Persona 10 (ES-001 to ES-005) is now complete.
>
> - *Business Development Manager phase (Persona 11 — BDM-001 to BDM-004)* — hosted in the existing business-development module (reuses its permissions, NotificationService, and target-account convert). BDM-001 territory plans (`bd_territory_plans`: geography/segments/named-accounts/partner-coverage/campaigns/pipeline+revenue targets) with a Sales-Head review notification + AI high-potential placeholder. BDM-002 market intelligence (`bd_market_signals`: configurable `bd-market-signal-type` competitor/pricing/objection/trend/opportunity, linkable to account/opportunity/campaign) + AI clustering placeholder. BDM-003 partner referral tracking (`bd_partner_referrals`: partner account + source + referred value + conversion + commission-eligibility flag). BDM-004 strategic opportunity creation reuses `convertBdTargetAccount`, now attributing the opportunity to a new `business_development` opportunity source and capturing use-case/product/priority. Three new tables + one option set seed for new tenants and backfill idempotently; BDM UI is isolated in `business-development-bdm.tsx`. AI items remain governed placeholders. Persona 11 (BDM-001 to BDM-004) is now complete.
>
> - *Sales Manager phase (Persona 12 — SMGR-001 to SMGR-006)* — an oversight surface that reuses existing modules rather than adding new ones, with all state in existing `metadata` JSONB (no migrations). SMGR-001 team pipeline (`/opportunities/manager/pipeline`: weighted pipeline, by-owner, aging buckets, by-stage, plus a heuristic high-risk list with AI placeholder). SMGR-002 lead SLA monitoring (`/sales-workspaces/manager/lead-sla`: assigned/accepted/overdue-first-contact/untouched/breached counts) with a manager `reassignLead` that logs the reason and fires an escalation notification to the new owner. SMGR-003 deal review keeps a coaching history in opportunity metadata (`/opportunities/:id/deal-review-log`). SMGR-004 discount approval reuses the approvals subsystem (list pending `discount_approval`, decide approve/reject, request-revision via comment — audit already logged). SMGR-005 forecast review adds a rep forecast category plus a manager override-with-reason (`/opportunities/:id/forecast`, `forecast-category` picklist) and forecast-vs-won comparison. SMGR-006 performance coaching computes per-rep win rate/conversion/avg deal size/cycle time and lets managers create coaching tasks (`/opportunities/:id/coaching-task`). Manager UI is surfaced on existing pages (`sales-manager-dashboard.tsx` on the opportunities page; `opportunity-manager-card.tsx` on the opportunity detail) with no new nav/RBAC plumbing. All AI items remain governed placeholders. Persona 12 (SMGR-001 to SMGR-006) is now complete.
>
> - *Sales Head / Revenue Leader phase (Persona 13 — SH-001 to SH-005)* — the executive layer. SH-001 to SH-004 live in a new `sales-leadership` module (reuses opportunity data + the new `sales_quotas` table; pure resolvers `computeQuotaAttainment`/`projectQuotaRisk`/`computeWinRate` are unit-tested). SH-001 revenue dashboard (`/sales-leadership/revenue-dashboard`: target vs achieved vs gap, pipeline, weighted forecast, win rate, avg deal size, sales cycle, renewal pipeline, by-team; region/product/segment/team/period filters via opportunity metadata + owner team). SH-002 quota management (`sales_quotas`: month/quarter/year, by product/region/segment/owner/team, `parent_quota_id` roll-up; attainment computed from closed-won and a deterministic risk projection). SH-003 strategic deal review board (open deals above a threshold with executive sponsor/business case/competitive risk/commercials/delivery risk/legal/next action in opportunity metadata + a leadership comment log). SH-004 win/loss analytics (closed deals grouped by loss reason/competitor/rep/product/segment/geography from native columns + metadata) with a governed `revive` action (reassign + flag + notify) for closed-lost deals. SH-005 sales-process governance wraps the existing configuration engine: RevOps proposes a draft, `submitVersionForReview` raises a new `configuration_change_approval`, and `decideVersionReview` publishes (versions + applies) on approval or leaves the draft on rejection — reusing versioning + approvals + notifications, surfaced via `configuration-governance.tsx` on the admin settings page. Two new migrations (`sales_quotas`, approval-type extension); leadership UI is isolated in `sales-leadership-dashboard.tsx` on the opportunities page (read-through plus quota create, leadership comments). All AI items (revenue summarization, quota-risk prediction, executive-intervention suggestions, win/loss pattern detection) remain governed placeholders. Persona 13 (SH-001 to SH-005) is now complete.
>
> - *Presales Consultant phase (Persona 14 — PS-001 to PS-005)* — extends the **existing presales module** (no new tables; all delivery state lives in `presales_requests.metadata` / `presales_requirements.metadata`, plus one configurable `presales-demo-checklist` option set). PS-001 request queue already showed opportunity/customer/owner/type/due/priority/stage; added a `triage` action (accept / reject / request-info) that notifies the sales owner, plus product surfacing. PS-002 demo workspace (pain points, use cases, audience, modules, competitors, objections, expected outcome, demo-flow notes + the configurable checklist) with an AI demo-flow placeholder. PS-003 demo feedback (attendees, modules shown, questions, objections, positive signals, gaps, next steps) with an optional opportunity **stage** update (reuses `OpportunityService.updateOpportunity`) and an AI sentiment placeholder. PS-004 solution fitment extends each requirement with customization/integration/dependency/risk (in requirement metadata, on top of the existing met/partial/gap compliance), turns gap requirements into **tasks or change requests** (`crm_tasks`), and requests sales/architect **review** (notification). PS-005 POC management (objective, scope, success criteria, timeline, responsibilities, demo data) with **sign-off** that captures customer feedback and updates opportunity **probability** (reuses `OpportunityService`). Hosted in `business-development.service.ts`/`createPresalesRouter`; one backfill migration for the demo checklist; presales UI is isolated in `presales-delivery-panels.tsx` on the presales page. AI items remain governed placeholders. Persona 14 (PS-001 to PS-005) is now complete.
>
> - *Solution Architect phase (Persona 15 — SA-001 to SA-005)* — a new `solution-architecture` module storing all state in `opportunity.metadata.architecture` (no new tables; pure resolvers `evaluateTechnicalDiscovery`/`summarizeDeliveryRisk`/`estimateIntegrationEffortDays` are unit-tested). SA-001 technical discovery captures the twelve technical fields (systems, integrations, APIs, authentication, data migration, hosting, security, compliance, users, concurrency, reporting, custom workflows) and **flags missing fields**; AI requirement extraction is a placeholder. SA-002 architecture recommendation (frontend/backend/database/integrations/AI layer/analytics/security/deployment/support) with architect **edit + approve** and a proposal link; AI drafting is a placeholder. SA-003 integration assessment (system/method/API availability/authentication/frequency/data direction/complexity/owner/risk) with a deterministic **effort estimate**, surfaced on the opportunity. SA-004 security questionnaire with **versioned** Q&A requiring architect approval; AI answer suggestions are a placeholder. SA-005 delivery-risk approval rolls up eight risk dimensions; **high risk requires leadership approval** via a new `delivery_risk_approval` (reuses the approvals subsystem) and the view reflects the approval decision, while a lightweight `architectureSummary` is surfaced on `OpportunityDetail` so risk/architecture status is visible on the opportunity. One migration (approval-type extension); architecture UI is isolated in `solution-architecture-panel.tsx` on the opportunity detail. AI items remain governed placeholders. Persona 15 (SA-001 to SA-005) is now complete.
>
> - *Proposal / Bid Manager phase (Persona 16 — PB-001 to PB-005)* — a new `proposals` module that keeps the per-opportunity proposal workspace in `opportunity.metadata.proposal` (no request table; pure resolvers `evaluateProposalCompliance`/`isProposalContentExpired`/`summarizeProposalVersions` are unit-tested) and adds **one** tenant-level table `proposal_content_library` for the cross-opportunity content library. PB-001 proposal request intake captures customer/opportunity/scope/due date/template (`opportunity-proposal-template`)/status (new `proposal-request-status` picklist)/contributors, **assigns contributors** (notifications) and **creates a deadline reminder** (`crm_tasks` reminder). PB-002 RFP compliance matrix stores each requirement with owner/response status/compliance status/comments/evidence and **flags missing responses** (AI extraction is a placeholder). PB-003 version control stores versions, **requires a `proposal_approval` before the final version can be locked** (reuses the approvals subsystem; the workspace reflects the approval decision) with a full audit trail. PB-004 content library (configurable `proposal-content-category`: product description/case study/security response/implementation methodology/pricing assumptions/company profile) **flags expired content**; AI recommendation is a placeholder. PB-005 submission tracking captures date/mode/recipient/documents/acknowledgement/remarks, **advances the opportunity stage to Proposal** (reuses `OpportunityService`) and **creates a follow-up task**. A cross-opportunity `/proposals/queue` lists every opportunity with a proposal workspace. One migration (content-library table + option-set backfill); proposal UI is isolated in `proposal-bid-panel.tsx` on the opportunity detail and `proposal-content-library.tsx` on the presales page. AI items remain governed placeholders. Persona 16 (PB-001 to PB-005) is now complete.
>
> - *Commercial / Finance Approver phase (Persona 17 — FIN-001 to FIN-004)* — a new `commercial` module keeping the per-opportunity commercial workspace in `opportunity.metadata.commercial` (no new tables; pure resolvers `computeQuoteTotals`/`evaluateDiscountApprovalRequirement`/`isNonStandardPaymentTerm`/`computePartnerCommission`/`detectQuoteDeviations` are unit-tested). Finance governance is deterministic — **no AI** in this persona. FIN-001 quote review captures line items (product/quantity/unit price/discount/tax), implementation + recurring fees, payment terms and margin, computes totals, **highlights deviations** (discount over the auto-ceiling, margin below minimum, non-standard terms), and lets finance approve / reject / request changes. FIN-002 discount governance uses a **configurable approval matrix** (`discount-approval-tier` option set whose value metadata carries `thresholdPct`/`requiresApproval`/`approverRole`); requests above the auto-approval ceiling raise a `discount_approval` (reuses the approvals subsystem) and the workspace reflects the decision, with full audit history. FIN-003 payment-term exceptions flag **non-standard terms** against the configurable `payment-term` set and raise a new `payment_terms_approval`. FIN-004 partner-commission validation computes commission from a finance-entered rate against the partner agreement, allows a **finance adjustment with reason**, links to the closed-won opportunity, and raises a new `partner_commission_approval` before payout. One migration (approval-type extension + payment-term/discount-tier option-set backfill); commercial UI is isolated in `commercial-finance-panel.tsx` on the opportunity detail. Persona 17 (FIN-001 to FIN-004) is now complete.
>
> - *Legal / Contract Reviewer phase (Persona 18 — LEG-001 to LEG-003)* — a new `legal` module keeping the per-opportunity legal-review workspace in `opportunity.metadata.legal` (no new tables; pure resolvers `evaluateLegalSla`/`evaluateLegalClauses`/`canApproveContract` are unit-tested). LEG-001 legal-review request captures contract type (configurable `legal-contract-type` set: NDA/MSA/SOW/DPA/order-form/amendment), due date, redlines, risk level and sales owner, **assigns a legal owner** (notification) and **starts an SLA timer** (computed `on_track`/`due_soon`/`breached`). LEG-002 clause deviation tracking tags clauses standard/modified/rejected/high-risk/accepted; a **high-risk clause requires leadership approval** via a new `legal_clause_approval` (reuses the approvals subsystem) and every decision is audit-logged. LEG-003 contract approval is **gated — it cannot be approved while unresolved high-risk clauses exist**; the approved contract links to the opportunity, a signed-version reference can be recorded, and **closure readiness is updated**. A lightweight `legalSummary` (risk/SLA/closure readiness/unresolved high-risk) is surfaced on `OpportunityDetail`. One migration (approval-type extension + contract-type option-set backfill); legal UI is isolated in `legal-review-panel.tsx` on the opportunity detail. Persona 18 (LEG-001 to LEG-003) is now complete.
>
> - *Partner Manager phase (Persona 19 — PM-001 to PM-005)* — **extends the existing `partners` module** rather than adding a new one (the module already had partner CRUD, deal registrations, onboarding tasks, and a dashboard). No new tables; new state lives in partner metadata + the existing `partner_deal_registrations` / `partner_onboarding_tasks` tables; one configurable `partner-onboarding-checklist` option set. Pure resolvers `computePartnerFitScore`/`detectChannelConflicts`/`computePartnerWinRate` are unit-tested. PM-001 partner application captures company/geography/industry/sales capacity/technical capability/customer base/certifications/references with a deterministic **fit score** (AI scoring stays a placeholder) and approve/reject/under-review decisions. PM-002 onboarding generates the configurable checklist as due-dated `partner_onboarding_tasks` (the due dates are the automated reminders, since `crm_tasks` has no partner entity type) with progress tracking + an owner notification. PM-003 deal-registration decisions add approve/reject/clarification with a **protection period** on approval. PM-004 channel-conflict resolution detects overlapping active registrations across partners (by account/customer), records a resolution, rejects the losing registrations, and **notifies affected partner owners**. PM-005 partner performance computes per-partner registrations/approvals/rejections/pipeline/revenue/win rate/cycle time/onboarding %/inactivity (AI high-performer detection stays a placeholder). New endpoints hang off `/partners` (`/management/conflicts`, `/management/performance`, `/:id/management`, `/:id/application`, `/:id/onboarding/generate`, `/:id/deals/:dealId/decision`); one migration (option-set backfill); partner UI is isolated in `partner-management-panel.tsx` and `partner-channel-panel.tsx` on the partners page. Persona 19 (PM-001 to PM-005) is now complete.
>
> - *Reseller / Partner Sales User phase (Persona 20 — RS-001 to RS-004)* — a new **partner-scoped `partner-portal` module** over the existing `partner_deal_registrations` data (no new tables; pure resolver `resolveCommissionStatus` is unit-tested). RS-001 portal session resolves the partners a user belongs to — by `owner_id` or `partner.metadata.portalUserIds` — so a reseller **sees only authorized partner records**; access is permission-gated (existing keys) and **login/access is audited** (`partner_portal.access`). RS-002 deal registration captures customer/contact/product/value/close date/notes/documents, runs a **duplicate check** against active registrations for the same customer, shows submission status, and **notifies the vendor owner** for approval. RS-003 collaboration lets the partner add note/task/meeting-request/demo-request items; the **internal team responds** via a separate edit-gated endpoint; the portal returns a **scoped, partner-safe view** (no owner/internal-only fields) and notifies the other side. RS-004 commission visibility derives status from the deal stage plus the linked opportunity's FIN-004 finance approval (`resolveCommissionStatus`: pending → eligible after approval → payout-pending/approved after closure) and lets the partner **raise a commission query** (notifies the owner). No migration; portal UI is isolated in `partner-portal-panel.tsx` (reseller view, self-hiding when the user has no portal access) on the partners page. Persona 20 (RS-001 to RS-004) is now complete.
>
> - *Support Agent L1 phase (Persona 21 — L1-001 to L1-005)* — **extends the existing `support` module** (support tickets already had channel/source, category/priority, SLA policies + computed SLA status/breach, `escalation_status`, `root_cause`, `resolution_notes`, and a knowledge base). No new tables; new state lives in ticket metadata + one configurable `support-root-cause` option set. Pure resolvers `suggestTicketClassification`/`rankKnowledgeArticles`/`computeQueueWeight` are unit-tested. L1-001 intake-assist flags **duplicate tickets** (same account, open, similar subject), provides a deterministic **category/urgency classification** (AI placeholder), and sends an **auto-acknowledgement** (customer reply). L1-002 SLA queue (`/support/queue`) sorts by SLA urgency (breached→at-risk→on-track, then priority) with **breach alerts** and counts. L1-003 KB recommendations rank knowledge articles by token overlap with the ticket text (AI placeholder), log **article usage + helpful feedback**, and link the article. L1-004 escalation requires reason/troubleshooting/logs/screenshots/impact/urgency, **assigns an L2 owner** (notification), sets `escalation_status='escalated'`, and optionally notifies the customer. L1-005 closure makes the **resolution summary mandatory**, captures a configurable **root-cause category**, sets resolved/closed by whether customer confirmation is requested, and **triggers the CSAT survey** (metadata flag + customer message). One migration (root-cause option-set backfill); L1 UI is isolated in `support-l1-panel.tsx` and `support-queue-panel.tsx` on the support page. AI items remain governed placeholders. Persona 21 (L1-001 to L1-005) is now complete.
>
> - *Support Agent L2 / Technical Support phase (Persona 22 — L2-001 to L2-004)* — **extends the same `support` module** (no new tables; L2 state lives in ticket metadata; reuses the knowledge base, ticket↔article links, and the approvals subsystem). Pure resolver `isRcaRequired` is unit-tested. L2-001 technical investigation workspace surfaces ticket detail + SLA + attachments + **prior tickets** (same account) and captures environment/configuration/logs + investigation notes; AI similar-issue summary is a placeholder. L2-002 bug escalation to engineering captures steps-to-reproduce/expected/actual/environment/logs/severity/customer-impact + an **engineering reference**, a visible **sync status** (open→acknowledged→in-progress→fixed/wont-fix→released), and **generates customer updates** from the status. L2-003 RCA captures root cause/impact/timeline/resolution/preventive-action/owner/due-date, is **mandatory for critical incidents** (enforced as a close gate on `closeTicket`), and can be **shared with the customer only after approval** via a new `rca_share_approval`. L2-004 converts a resolved ticket into a **draft** knowledge article linked to the ticket, which then **requires review before publishing** (`publishKnowledgeArticle`); AI formatting is a placeholder. One migration (approval-type extension); L2 UI is isolated in `support-l2-panel.tsx` on the support page. AI items remain governed placeholders. Persona 22 (L2-001 to L2-004) is now complete.
>
> - *Support Manager phase (Persona 23 — SPM-001 to SPM-005)* — **extends the same `support` module** (no new tables; manager state lives in ticket metadata + one configurable `support-breach-reason` option set; reuses teams, SLA policies + `computeSlaStatus`, escalation status, notifications, and the shared dashboard). Pure resolvers `computeAgentPerformance`/`computeSlaCompliance`/`computeWorkload`/`detectOverload`/`resolveCsatBand` are unit-tested. SPM-001 team performance (`/support/management/performance`) rolls up per-agent + team **assigned/open/resolved, SLA compliance %, avg first-response & resolution time, reopened count, and CSAT average** over the team scope. SPM-002 workload balancing (`/support/management/workload`) shows per-agent open/at-risk/breached load against a **configurable capacity** with an **overload flag**, plus a bulk `/support/management/reassign` that reuses the existing assignment gate + notification + audit. SPM-003 SLA-breach governance records a configurable **breach reason** + corrective action per ticket (`/support/tickets/:id/breach-review`). SPM-004 escalation oversight (`/support/management/escalations`) lists escalated tickets with **aging**; the manager can **reassign the escalation owner** or **return-to-L1** (`/support/tickets/:id/escalation-review`). SPM-005 CSAT capture (`/support/tickets/:id/csat`) records a 1–5 score + comment completing the L1-triggered survey, feeding **real CSAT aggregates** into the shared dashboard (the `csatPlaceholder` stays for back-compat; a new `csat` field carries the numbers). One migration (breach-reason option-set backfill); manager UI is isolated in `support-management-panel.tsx` on the support page (self-hiding without assign/dashboard permission). AI performance insights remain governed placeholders. Persona 23 (SPM-001 to SPM-005) is now complete.
>
> - *Customer Success Manager — Onboarding phase (Persona 24 — CSMO-001 to CSMO-005)* — **extends the existing `customer-success` module** (no new tables/migration; onboarding state lives in `onboarding_plans.metadata`, reusing `onboarding_plans`/`onboarding_milestones`, `customer_health_scores`, and the existing `cs-segment`/`customer-success-stage` option sets). Pure resolvers `validateHandover`/`resolveOnboardingTemplate`/`computeGoLiveReadiness`/`generateKickoffAgenda` are unit-tested. CSMO-001 sales-to-CS handover captures contract/scope/products/commitments/stakeholders/timeline/risks/special-terms/integrations/success-criteria; **missing fields are flagged** and onboarding **cannot start until mandatory fields are complete** (`validateHandover` gate on kickoff). CSMO-002 closed-won deals **auto-create an onboarding project** — `closeOpportunityWon` calls `provisionOnboardingFromOpportunityWithClient` inside the same transaction (idempotent per opportunity), creating/finding the CS account, seeding milestones from a product/segment/implementation-type **template**, and pre-filling the handover from the close-won data; a manual `POST /customer-success/onboarding/provision` mirrors it. CSMO-003 kickoff generates a standard **agenda**, captures attendees/decisions/action-items and confirms success criteria (AI summary is a governed placeholder). CSMO-004 go-live readiness tracks an 8-item checklist (configuration, data migration, access, training, integrations, testing, sign-off, support plan) with a **readiness score**; go-live is **blocked while critical items are pending** (`computeGoLiveReadiness` gate). CSMO-005 completion captures go-live date/users-trained/adoption-baseline/open-risks/pending-items/sign-off, **assigns the ongoing CSM**, moves the CS account to the adoption stage, and **initializes the health score** (`customer_health_scores` + denormalized account score). No migration; onboarding UI is isolated in `cs-onboarding-panel.tsx` on the customer-success page. AI items remain governed placeholders. Persona 24 (CSMO-001 to CSMO-005) is now complete.
>
> - *Customer Success Manager — Scaled phase (Persona 25 — CSMS-001 to CSMS-005)* — **extends the existing `customer-success` module** (one new `cs_adoption_campaigns` table for CSMS-002; everything else reuses `customer_health_scores`, `escalations`, `renewals`, `crm_tasks`, and `opportunities`). Pure resolvers `computeHealthScore`/`resolveHealthBand`/`detectLowUsage`/`detectExpansionSignals`/`matchesCampaignTarget` are unit-tested. CSMS-001 automated health score computes a weighted 0–100 score from 10 governed factors (usage, login activity, support tickets, SLA breaches, CSAT, NPS, training completion, renewal proximity, payment status, engagement), bands it **green/amber/red**, records it to `customer_health_scores` (drivers + factors in history) and updates the account risk (`POST /accounts/:id/health-score/compute`); AI driver explanation is a governed placeholder over the deterministic drivers. CSMS-002 adoption campaigns (`/adoption-campaigns`) target customers by usage/module/role/segment/health band (`matchesCampaignTarget`), estimate a live target count, and carry engagement/adoption-improvement metrics (AI content/target recommendation is a placeholder). CSMS-003 low-usage alerts (`POST /accounts/:id/low-usage-check`) open a **tracked risk** (escalation) with an auto-created recovery **playbook** + suggested comm template when usage drops below threshold (`detectLowUsage`, severity-scaled). CSMS-004 renewal reminders (`POST /accounts/:id/renewal-playbook`) create the **renewal** plus cross-functional `crm_tasks` for CSM/sales/finance/customer-contact and return the health score + open-issue count. CSMS-005 expansion signals (`POST /accounts/:id/expansion/assess`) detect high-usage/departments/feature-requests/user-growth/cross-module-queries/engagement (`detectExpansionSignals`); a recommendation can **create a pipeline opportunity** (`source=expansion`) and **notify the sales owner** (`POST /accounts/:id/expansion/opportunity`) — done via direct SQL to avoid a customer-success↔opportunities import cycle. One migration (`cs_adoption_campaigns`); scaled UI is isolated in `cs-scaled-panel.tsx` on the customer-success page. AI items remain governed placeholders. Persona 25 (CSMS-001 to CSMS-005) is now complete.
>
> - *Customer Success Manager — Enterprise phase (Persona 26 — CSME-001 to CSME-005)* — **extends the existing `customer-success` module** (no new tables; enterprise state lives on `success_plans`/`qbrs`/`escalations`/`renewals` rows + `customer_success_accounts.metadata`). Pure resolvers `validateSuccessPlan`/`requiresLeadershipEscalation`/`predictRenewalProbability`/`computeAdvocacyReadiness` are unit-tested. CSME-001 strategic success plan captures objectives/stakeholders/success-metrics/adoption-roadmap/milestones/risks/renewal-date/expansion (stored on a `success_plans` row), flags missing sections, and supports a review-with-customer marker (AI plan suggestion is a placeholder). CSME-002 executive business review schedules a QBR/EBR (`/enterprise/qbrs`) and records the review content (adoption/outcomes/support/ROI/roadmap/risks/next-steps) + tracked **action items** on the qbr (AI summary is a placeholder). CSME-003 strategic risk (`/enterprise/risks`) requires an owner + mitigation plan, tags one of seven risk types on an escalation, and **escalates high/critical risks to leadership** via notification. CSME-004 renewal strategy (`/enterprise/renewal-strategy`) records the renewal plan + **AI-stand-in probability** (`predictRenewalProbability`) and notifies the sales collaborator. CSME-005 advocacy (`/enterprise/advocacy/assess` + `/request`) scores advocacy readiness from health/NPS/adoption/renewal/exec-relationship and creates an advocacy request with **consent tracking** in account metadata. No migration; enterprise UI is isolated in `cs-enterprise-panel.tsx`. AI items remain governed placeholders. Persona 26 (CSME-001 to CSME-005) is now complete.
>
> - *Customer / Prospect Portal User phase (Persona 27 — CP-001 to CP-004)* — **extends the existing `customer-portal` module** (no new tables; reuses `leads`, `knowledge_articles`, `customer_feedback`, `onboarding_plans`/`onboarding_milestones`). CP-001 demo request (`POST /customer-portal/demo-request`) **creates a lead** (product/preferred-date/org-type/contact) with a confirmation message; the new lead is the inside-sales queue alert. CP-002 support-ticket submission already existed (category/priority/attachments, ticket number, SLA/status, message updates). CP-003 knowledge access adds **article rating** (`POST /customer-portal/knowledge/:id/rate`, helpful yes/no) on top of the existing search + Ask-AI, with entitlement enforced by the customer-visible knowledge filter. CP-004 onboarding task completion (`GET /customer-portal/onboarding`, `POST /customer-portal/onboarding/:milestoneId/complete`) surfaces the account's onboarding-plan milestones (label/due/owner/instructions), lets the customer **upload a document ref + mark complete**, **notifies the CSM**, and updates the onboarding project (milestone status). New portal UI: `customer-portal-onboarding-page.tsx` (+ nav), article-rating on the knowledge page, and a demo-request card on the portal dashboard. No migration. Persona 27 (CP-001 to CP-004) is now complete.
>
> - *CRM Administrator phase (Persona 28 — ADMIN-001 to ADMIN-006)* — **this persona is delivered almost entirely by the existing configuration engine + admin modules; the phase closes the remaining gaps rather than rebuilding.** ADMIN-001 (RBAC roles/object-field-record-action permissions/teams + audit) is the `rbac` module; ADMIN-002 (BPF builder: stages/steps/required-fields/validation/transitions, assignable, stage logging, mandatory-stage enforcement with override) is `business_process_flow` configuration definitions + the `bpf` runtime (`validateBpfTransition`); ADMIN-003 (workflow automation: trigger/condition/action/history) is the `workflows` module whose action vocabulary already includes create-task, send-notification, assign-owner, update-field, `trigger_approval`, `create_support_ticket`, and `run_ai_prompt`/`run_ai_agent`; ADMIN-004 (approval matrices: value/discount/region/product/role/risk/tier, sequential/parallel, SLA/escalation, immutable logs) is `approval_matrix` definitions + the `approvals` runtime; ADMIN-005 (fields required by stage/role, persona layouts, versioned) is `object`/`page_layout` definitions. **The two real gaps closed here:** (1) ADMIN-006 *"changes require reason"* — configuration draft creation previously accepted an empty reason; the `/configuration/versions` schema and `createDraft` now **require** a trimmed reason (min 3 chars). (2) A CRM-Administrator **configuration console** (`admin-configuration-page.tsx` at `/admin/configuration`, +nav) exposes the full versioning lifecycle to admins — create draft (reason required), **validate current config** (surfacing error/warning issues), **publish** a draft, **roll back** to a published version, and a hub linking to RBAC/Workflows/Approvals/Fields/Modules/BPFs. The existing `configuration-governance.tsx` now also enforces the required reason. No migration; everything reuses the configuration versioning/validation/audit engine. Persona 28 (ADMIN-001 to ADMIN-006) is now complete.
>
> - *System Administrator phase (Persona 29 — SYS-001 to SYS-004)* — a new **`system` module**; SYS-002 (audit log viewer) was already served by the existing `audit` module (`/logs` searchable, permission-gated `/export`, append-only `audit_logs` capturing actor/action/resource/IP/user-agent/timestamp/metadata). Pure resolvers `computeIntegrationHealth`/`requiresDeploymentApproval`/`canDeploy`/`computeBackupStatus` are unit-tested. SYS-001 integration monitoring (`/system/integrations`) registers connections and their **sync-run history** (status/failures/retries/latency/last-sync/errors), aggregates a health summary, **raises an alert on failure** (flips the connection to `error`), and supports **retry** (a linked fresh run) and **mark-resolved**; logs are retained in `integration_sync_runs`. SYS-003 environment management (`/system/environments`) labels dev/test/staging/production environments and tracks **deployments** with a documented **rollback plan**; **production deployments require approval** — they start `pending_approval` and cannot be `deploy`-ed until `approve`-d (enforced by `requiresDeploymentApproval`/`canDeploy`), with approve/reject/deploy/rollback all audit-logged. SYS-004 backup & recovery (`/system/backups`) configures the **schedule + RPO/RTO** (`backup_policies`), records backup and **restore-test** runs (`backup_runs`), surfaces posture (last backup, **RPO breach**, failures) via `computeBackupStatus`, and **alerts on backup failure**. One migration (`integration_connections`, `integration_sync_runs`, `environments`, `environment_deployments`, `backup_policies`, `backup_runs`); System-Admin UI in `admin-system-page.tsx` at `/admin/system` (+nav). Persona 29 (SYS-001 to SYS-004) is now complete.
>
> - *AI Governance Manager phase (Persona 30 — AIG-001 to AIG-005)* — a new **`ai-governance` module** built on top of the existing AI stack; AIG-002 (human approval of sensitive AI actions) and AIG-004 (AI audit trail — prompt/context/output logging + redaction masking + searchable logs) were already served by the `ai-actions` (`ai_action_runs.requires_review`/review) and `ai-gateway` modules. Pure resolvers `requiresUseCaseApproval`/`resolveAiExplanation`/`computeAiQuality` are unit-tested. AIG-001 AI use-case registry (`/ai-governance/use-cases`) captures use case/owner/object/persona/data-used/action-type/**risk level**/approval-status/model/prompt/monitoring-plan; **high-risk use cases start pending approval** (`requiresUseCaseApproval`) and every change **increments a version** with a snapshot (`ai_use_case_versions`), with submit/approve/reject decisions audit-logged. AIG-003 AI explanation (`resolveAiExplanation`) surfaces **top positive/negative factors** and **flags low-confidence** outputs, and `/ai-governance/feedback` captures user feedback incl. **hallucination reports** and confidence flags. AIG-005 AI quality dashboard (`/ai-governance/quality`, `computeAiQuality`) shows usage, **override rate** (rejected reviews), low-confidence count, feedback/helpful rate, **hallucination reports**, avg response time, and a composite **business-impact** score; **AI issues become improvement tasks** (`/ai-governance/improvement-tasks` → `ai_improvement_tasks`). One migration (`ai_use_cases`, `ai_use_case_versions`, `ai_feedback`, `ai_improvement_tasks`); AI-governance UI in `ai-governance-page.tsx` at `/ai-governance` (+nav). Persona 30 (AIG-001 to AIG-005) is now complete.
>
> - *Data Quality Manager phase (Persona 31 — DQM-001 to DQM-004)* — a new **`data-quality` module** (lead-focused). Pure resolvers `isValidEmail`/`isValidPhone`/`computeCompleteness`/`isStale`/`validateImportRows`/`resolveMergedRecord` are unit-tested. DQM-001 dashboard (`/data-quality/dashboard`) computes **completeness, duplicate emails, invalid emails/phones, missing owners, missing sources, stale leads, and consent gaps** live from the leads table, ranks deterministic **cleanup priorities** (AI ranking is a placeholder), and returns sample record IDs for **drill-in**. DQM-002 import validation (`/data-quality/import/validate`) checks **mandatory fields, format, allowed values, consent, and in-batch duplicates** and returns per-row errors (client downloads a CSV **error report**); `/import/commit` inserts only valid rows and **logs the batch** (`dq_import_batches`). DQM-003 enrichment queue (`/data-quality/enrichment`) scans incomplete leads into `dq_enrichment_queue` with deterministic **suggestions** (AI placeholder), and accept/reject/edit applies the values back to the record with **source + confidence** stored. DQM-004 merge (`/data-quality/merge`) picks a master, does **field-level merge** (`resolveMergedRecord`), **reassigns related activity** (crm_tasks) and preserves the duplicate's data in `dq_merge_log`, soft-deletes the duplicate, and **requires a merge reason**. One migration (`dq_enrichment_queue`, `dq_merge_log`, `dq_import_batches`); data-quality UI in `data-quality-page.tsx` at `/data-quality` (+nav). Persona 31 (DQM-001 to DQM-004) is now complete.
>
> - *Executive / CEO / CXO phase (Persona 32 — EXE-001 to EXE-003)* — a new **`executive` module** (the final persona, completing the 32-persona catalog). Pure resolvers `computeExecWinRate`/`computeWeightedForecast`/`computeRoi`/`detectExecutiveInsights` are unit-tested. EXE-001 command center (`/executive/command-center`) computes **revenue, pipeline, weighted forecast, win rate, campaign ROI, avg customer health, renewal-risk accounts, support-SLA attainment, partner revenue, and open strategic risks** live from opportunities/campaigns/customer-success/support/strategic-risks, with **period/region/product/team drill filters** and an AI executive-summary placeholder. EXE-002 AI business insights (`/executive/insights`, `detectExecutiveInsights`) flags **pipeline gaps, forecast risk, churn risk, sales underperformance, campaign inefficiency, and partner inactivity**, each with a **recommended action** and suggested owning leader; `/executive/insights/assign` **assigns an action to a leader** (notification + `executive_insight_actions`). EXE-003 strategic risk register (`/executive/risks`) tracks risks from **opportunities/customers/support/renewals/AI-alerts/manual** with **owner, severity, impact, mitigation, due date, and status**; leadership monitors open risks (severity-ordered). One migration (`strategic_risks`, `executive_insight_actions`); executive UI in `executive-page.tsx` at `/executive` (+nav). Persona 32 (EXE-001 to EXE-003) is now complete — **all 32 CRM personas are delivered.**
>
> - *Cross-functional user stories phase (CF-001 to CF-010)* — a new **`cross-functional` module** for the record-level stories that span every persona; CF-002 (unified activity timeline), CF-003 (task management), CF-007 (SLA — support), and CF-008 (approval workflow) were already served by the `crm` (timeline/tasks/notes), `support` (SLA), and `approvals` modules. Pure resolvers `computeAttribution`/`recommendNextBestAction`/`extractMentions` are unit-tested. CF-001 lead source attribution (`/cross-functional/leads/:id/attribution`) captures source/sub-source/campaign/UTM/partner/event/referral touches and computes **first-touch, last-touch, and linear multi-touch** attribution (`computeAttribution`); source cannot be blank. CF-004 meeting intelligence (`/cross-functional/:entity/:id/meetings`) records **summary, decisions, objections, next steps, stakeholders, and sentiment**, editable before saving, linked to the record (AI generation is a placeholder). CF-005 next best action (`/cross-functional/:entity/:id/next-best-action`, `recommendNextBestAction`) recommends **call/email/meeting/proposal/manager-review/nurture/escalation/closure** with a reason from live signals, and users **accept/dismiss/snooze**. CF-006 record ownership (`/cross-functional/:entity/:id/ownership`) reassigns owners across lead/account/opportunity/ticket/CS with a **mandatory reason** and a tracked **ownership-change history**. CF-009 document management (`/cross-functional/:entity/:id/documents`) links tagged documents with **version history** and **locks final approved documents** (locked docs reject new versions). CF-010 internal collaboration (`/cross-functional/:entity/:id/comments`) posts comments with **@mentions** (notified) and an **internal flag hidden from portal users**. One migration (`cf_attribution_touches`, `cf_meeting_summaries`, `cf_next_best_actions`, `cf_ownership_changes`, `cf_documents`, `cf_document_versions`, `cf_record_comments`); UI in `record-tools-page.tsx` at `/record-tools` (+nav). Cross-functional stories CF-001 to CF-010 are now complete.
>
> - *AI-specific user stories phase (AI-001 to AI-010)* — a new **`ai-agents` module** exposing ten governed AI agents through one **run → decide** contract; every run carries **confidence, an explanation (factors), sources, a low-confidence flag, and a human-review gate**, and supports **accept / override / reject** with feedback written to the Persona-30 `ai_feedback` table (so outcomes improve future scoring — AI-002 — and triage accuracy is stored — AI-008). New unit-tested resolvers `enrichLead`/`scoreLead`/`draftEmail`/`summarizeCall`/`draftProposal`/`assessOpportunityRisk`/`forecastOpportunity`; AI-008/009/010 reuse the existing `suggestTicketClassification`, `computeHealthScore`+`resolveAiExplanation`, and `rankKnowledgeArticles` resolvers. AI-001 lead enrichment suggests company/industry/size/website/role/pain-points/talking-points with source + confidence and requires approval. AI-002 lead scoring weights structured signals into a hot/warm/cold score with an explanation, override, and stored feedback. AI-003 email drafting produces a subject/body from CRM context with a selectable tone/template, **requires review before sending**, and **logs an email activity to the timeline on accept**. AI-004 call summary structures notes into pain-points/questions/objections/next-steps/sentiment/follow-up, editable, and **links a call activity to the timeline**. AI-005 proposal drafting builds sections from approved templates, **marks assumptions + missing information**, and **requires human approval** before sharing. AI-006 opportunity risk flags inactivity/slippage/missing-DM/competitor/price-objection/demo-gap/low-engagement with a level + recommended action. AI-007 forecasting predicts close probability + expected revenue, shows factors, and **compares AI vs rep forecast** (variance). AI-008 support triage classifies category/urgency/priority/sentiment/suggested-owner with override + accuracy feedback. AI-009 customer health evaluates factors into a banded score with explanation + recommended playbook. AI-010 knowledge assistant answers **only from approved knowledge sources** with visible sources, **low-confidence flagging**, and human review. One migration (`ai_agent_runs`); AI-agents UI in `ai-agents-page.tsx` at `/ai-agents` (+nav). AI execution remains a governed deterministic placeholder until the AI Gateway phase. AI-specific stories AI-001 to AI-010 are now complete.
>
> See [configuration-engine.md](./configuration-engine.md). These precede the per-entity work below because they make every subsequent phase publishable, validatable, reversible, and live-applicable. Phases A–G remain as written; they now plug into the snapshot/version/validate/apply foundation.

## Guiding principles

1. **Additive only** — new tables and endpoints extend the existing `tenant-config` module; no destructive migrations, no table drops, no rewrites.
2. **Backward-compatible fallback** — every new config layer is *optional*. When a tenant has no config rows, services behave exactly as they do today (code catalogs / Zod schemas). Existing tenants are unaffected until they opt in.
3. **Config-as-data, never hard-coded** — automations, validations, layouts, and dashboards become tenant data referenced by key; code provides defaults and execution, not business rules.
4. **One write-path per concern** — reuse `system_settings` (KV) and the existing definition tables; add a new table only when a blob can't model relationships or query needs.
5. **Tenant-scoped, audited, soft-deletable** — match the conventions already on every config table (`tenant_id`, `deleted_at`, `created_by/updated_by`, `updated_at` trigger).
6. **Ship behind reads first** — land schema + read APIs + seed-from-code before any UI/rendering, so nothing breaks mid-flight.

---

## Phased plan (priority order)

Effort is rough (S ≤ 2d, M ≈ 3–5d, L ≈ 1–2w). Each phase is independently shippable.

### Phase A — Make custom fields functional end-to-end **(highest value, M)**

*Closes gap #1 (and unblocks #2, #3).*

- **Schema (additive):** add custom-field **value** storage. Preferred: a per-entity `custom_fields JSONB` column on business tables (e.g. `leads`, `accounts`, `opportunities`) — least invasive, no joins. Alternative: a single `custom_field_values` EAV table (`tenant_id`, `entity_key`, `record_id`, `field_id`, `value`) if you need typed querying.
- **Service:** in each entity's create/update path, read `custom_field_definitions` for that `entity_key` and read/write the values; ignore unknown keys. No business table is restructured.
- **Read API:** expose effective field set (system + custom) per entity so the frontend can render.
- **Backward-compat:** entities with no custom-field definitions behave exactly as today.
- **Tests:** extend the tenant-config exhaustive script; add an API test that a defined field round-trips on a record.

### Phase B — Configurable validations **(M)**

*Closes gap #3; depends on A.*

- **Schema:** reuse the existing `custom_field_definitions.settings` JSONB for rules (`required`, `min`, `max`, `minLength`, `maxLength`, `regex`, `options`); optionally add a `validation_rules` table for cross-field/entity-level rules.
- **Service:** a single server-side validator reads rules and enforces them in entity create/update, returning the platform's standard `VALIDATION_ERROR` shape. Zod stays as the transport-level guard; config rules layer on top.
- **Backward-compat:** no rules ⇒ no extra enforcement.

### Phase C — Form-layout write + dynamic rendering **(M)**

*Closes gap #2; depends on A.*

- **API:** add `POST/PUT/DELETE /tenant-config/form-layouts` to the existing router/service (`custom_form_layouts` already stores `layout_schema`). Mirror the custom-field permission gates (`admin.create/edit/delete/configure`).
- **Frontend:** a schema-driven form renderer that consumes `layout_schema` (sections → fields, system + custom) with a safe default layout when none exists.
- **Backward-compat:** no layout ⇒ current static forms.

### Phase D — Data-driven dashboards **(L)**

*Closes gap #4.*

- **Schema:** `dashboard_definitions` and `dashboard_widgets` (tenant-scoped), plus a **metric registry** that maps a `metricKey` to a vetted, parameterised query (metrics stay code-defined for safety; *composition* becomes data).
- **Service:** `dashboard.service` resolves a tenant's dashboards from data and falls back to the current code `dashboardCatalog` when none exist. `dashboard_saved_views` continues to work unchanged.
- **Guardrail:** widgets reference registered `metricKey`s only — no arbitrary SQL from config.

### Phase E — Extensible module & permission catalog **(M)**

*Closes gap #5.*

- **Schema:** a `module_registry` table seeded from today's `permissionModuleKeys`; permissions continue to live in `permissions` but can be **registered** for a new module key as data.
- **Service:** RBAC and module-toggle read the registry (seeded from the code enum, so current behaviour is identical) — enabling new modules to appear without editing the enum.
- **Backward-compat:** the code enum becomes the *seed*, not the only source.

### Phase F — Workflow auto-dispatch **(L, infra-gated)**

*Closes gap #6; aligns with the deferred "background workers" in `POST_RELEASE_ROADMAP.md`.*

- **Approach:** emit domain events from entity services to an event/outbox table; a worker matches active workflows by `trigger_type` + conditions and executes the existing action pipeline (already implemented for manual runs). Honour the existing human-approval gates for sensitive AI actions.
- **Guardrail:** ships disabled by default (`BACKGROUND_WORKERS_ENABLED=false`), so nothing auto-fires until explicitly enabled.

### Phase G — Metadata-driven custom objects **(L+, long-term, optional)**

*Closes gap #7.* Define brand-new objects (not just fields) as configuration. Large initiative (dynamic schema/storage, listing, permissions, API generation). **Recommend deferring** until A–E prove the configuration model; document as a north-star, not a near-term commitment.

---

## Sequencing summary

```
A (custom-field values)  ──►  B (validations)
        │                         └─► C (form layouts + renderer)
        ▼
   D (dashboards)   E (module/permission registry)   F (workflow auto-dispatch)
                                                        │
                                                        ▼
                                              G (custom objects — long-term)
```

A is the keystone (it unblocks B and C and delivers visible value fastest). D, E, F are independent and can run in parallel after A. G is intentionally last/optional.

---

## Recommended next phase

**Continue Phase A after the Lead + Account + Opportunity + Contact slices** by expanding the same custom-field persistence pattern to the next high-value CRM entities and then consuming the same field-definition contract in rendering:

1. Replicate the additive `custom_fields JSONB` pattern to the next CRM entities that already have custom-field metadata demand — Contacts, Support tickets, and Partners are now done (the last two on the shared `apps/api/src/common/custom-fields.ts` engine), so continue with the remaining commercially active modules (e.g. resellers, customer-success accounts, renewal/expansion) where the business case is strongest, reusing the shared engine.
2. Reuse the lead/account/opportunity `fieldDefinitions` + custom option-catalog contract as the schema-driven read path for those next entity form/detail screens instead of introducing new one-off payload shapes.
3. Once the read path is proven across more than one entity, layer Phase B validation rules and Phase C form-layout rendering on the same contract instead of inventing a separate field schema.
4. Keep extending the CRM exhaustive suite so each entity proves a custom field round-trips through create, update, options, and detail reads.

This keeps the momentum of the lead slice, reuses the same additive approach, and turns the new field-definition/value contract into a platform pattern rather than a one-off implementation.

**Explicitly not in the next phase:** new CRM business modules, dashboards-as-data, custom objects, or any change to existing tables' current columns.
